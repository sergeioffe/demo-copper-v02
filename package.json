{
  "name": "copper-demo-v02",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "server",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=apps/client",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --build",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "patch-package": "^8.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
